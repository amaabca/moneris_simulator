require 'spec_helper'

describe MonerisSimulator do
  it 'has a version number' do
    expect(MonerisSimulator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
